{
  "name": "mern-starter-stack",
  "version": "1.0.0",
  "main": "index.js",
  "author": "arkine <slyngstad55@gmail.com>",
  "license": "MIT",
  "scripts": {
    "dev": "concurrently --raw --kill-others \"cd server && yarn dev\" \"cd client && yarn dev\"",
    "build:dev": "cd client && yarn build:dev",
    "build": "cd client && yarn build",
    "prisma:deploy": "cd server && yarn prisma:deploy"
  },
  "dependencies": {
    "concurrently": "^4.0.1"
  },
  "devDependencies": {
    "eslint": "^5.10.0"
  }
}
